from framefox.core.routing.decorator.route import Route
from framefox.core.controller.abstract_controller import AbstractController
from typing import Dict


class {{ controller_class_name }}(AbstractController):
    @Route("/{{ controller_file_name }}", "{{ controller_file_name }}.data", methods=["GET"])
    async def data(self) -> Dict:
        """GET /api/{{ controller_file_name }}/data - Sample data endpoint"""
        return self.json({
            "data": {
                "controller_name": "{{ controller_class_name }}",
                "timestamp": "{{ timestamp }}",
                "sample_data": []
            },
            "status": "success"
        }, status=200)
