# ORM configuration for the application

database:
  url: "${DATABASE_URL}" # Replace with your database URI (e.g., PostgreSQL, MySQL)
  echo: false # Enables SQL logging (useful for development)
  autocommit: false
  autoflush: false
  # Add other configuration parameters if necessary