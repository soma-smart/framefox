---
title: Guide de démarrage rapide
description: Commencez à développer avec Framefox, le framework web Python moderne et productif.
template: splash
hero:
  tagline: Développez des applications web Python rapidement et efficacement !
  image:
    file: ../../assets/houston.webp
  actions:
    - text: Commencer maintenant
      link: /docs/installation
      icon: right-arrow
    - text: Voir les exemples
      link: /docs/controllers
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Qu'est-ce que Framefox ?

**Framefox** est un framework web Python moderne, conçu pour simplifier le développement d'applications web robustes et maintenables. Basé sur **FastAPI**, il adopte une approche orientée objet avec une architecture **MVC claire** pour améliorer la productivité des développeurs.

### Caractéristiques principales

<CardGrid stagger>
	<Card title="Architecture MVC" icon="setting">
		Structure claire avec séparation des responsabilités : Modèles, Vues et Contrôleurs.
	</Card>
	<Card title="Basé sur FastAPI" icon="rocket">
		Performance élevée avec validation automatique des types et documentation OpenAPI.
	</Card>
	<Card title="Terminal interactif" icon="laptop">
		Commandes intégrées pour générer rapidement contrôleurs, entités et formulaires.
	</Card>
	<Card title="Système de routing avancé" icon="star">
		Décorateurs de routes simples avec noms et méthodes HTTP personnalisables.
	</Card>
</CardGrid>

## Installation

Installez Framefox via pip :

```bash
pip install framefox
```

Créez un nouveau projet :

```bash
mkdir mon-projet-framefox
cd mon-projet-framefox
framefox init
```

Démarrez le serveur de développement :

```bash
framefox serve
```

Votre application est maintenant accessible sur `http://localhost:8000` ! 🎉

## Structure du projet

Un projet Framefox suit une structure organisée :

```
mon-projet/
├── src/
│   ├── controllers/       # Contrôleurs de votre application
│   ├── entities/          # Modèles de données (ORM)
│   ├── forms/             # Formulaires et validation
│   ├── repository/        # Couche d'accès aux données
│   └── templates/         # Templates Jinja2
├── public/                # Assets statiques (CSS, JS, images)
├── config/                # Configuration de l'application
├── requirements.txt       # Dépendances Python
└── main.py               # Point d'entrée de l'application
```

## Votre premier contrôleur

Créez un contrôleur avec le terminal interactif :

```bash
framefox create controller
```

Ou créez manuellement un fichier `src/controllers/home_controller.py` :

```python
from framefox.core.routing.decorator.route import Route
from framefox.core.controller.abstract_controller import AbstractController

class HomeController(AbstractController):
    @Route("/", "home.index", methods=["GET"])
    async def index(self):
        return self.render("home/index.html", {
            "message": "Bienvenue avec Framefox !"
        })
    
    @Route("/users/{id}", "home.user", methods=["GET"])
    async def show_user(self, id: int):
        return {"user_id": id, "name": f"Utilisateur {id}"}
```

### Les décorateurs Route

Le décorateur `@Route` définit les routes de votre application :

- **Premier paramètre** : Le chemin URL (`/`, `/users/{id}`)
- **Deuxième paramètre** : Le nom de la route (`home.index`)
- **methods** : Les méthodes HTTP autorisées (`["GET", "POST"]`)

## Rendu de templates

Framefox utilise **Jinja2** pour le rendu des templates. Créez `src/templates/home/index.html` :

```html
<!DOCTYPE html>
<html>
<head>
    <title>Mon App Framefox</title>
    <link href="{{ asset('css/style.css') }}" rel="stylesheet">
</head>
<body>
    <h1>{{ message }}</h1>
    <p>URL vers utilisateur : <a href="{{ url_for('home.user', id=123) }}">Utilisateur 123</a></p>
    
    <!-- Token CSRF automatique -->
    <form method="POST">
        {{ csrf_token() }}
        <input type="text" name="username" placeholder="Nom d'utilisateur">
        <button type="submit">Valider</button>
    </form>
</body>
</html>
```

### Fonctions de template intégrées

- `{{ url_for('route.name', param=value) }}` : Génère des URLs
- `{{ asset('path/file.css') }}` : Liens vers les assets statiques
- `{{ csrf_token() }}` : Protection CSRF automatique
- `{{ current_user }}` : Utilisateur authentifié
- `{{ get_flash_messages() }}` : Messages flash

## Gestion des formulaires

Créez des formulaires typés avec validation automatique :

```python
from framefox.core.form.form_type import FormType
from framefox.core.form.field.text_type import TextType
from framefox.core.form.field.email_type import EmailType

class UserType(FormType):
    def build_form(self, form_builder):
        form_builder.add("name", TextType, {
            "required": True,
            "label": "Nom complet"
        })
        form_builder.add("email", EmailType, {
            "required": True,
            "label": "Adresse e-mail"
        })
```

Utilisez le formulaire dans votre contrôleur :

```python
@Route("/register", "user.register", methods=["GET", "POST"])
async def register(self, request: Request):
    form = self.create_form(UserType)
    
    if request.method == "POST":
        await form.handle_request(request)
        if form.is_valid():
            # Traiter les données
            data = form.get_data()
            return self.redirect("user.success")
    
    return self.render("user/register.html", {"form": form})
```

## Authentification et sécurité

Framefox inclut un système d'authentification complet :

### Configuration des firewalls

Dans `config/security.yaml` :

```yaml
security:
  firewalls:
    main:
      pattern: ^/
      authenticator: form_login
      login_path: /login
      check_path: /login_check
      default_target_path: /dashboard
      
  access_control:
    - path: ^/admin
      roles: [ROLE_ADMIN]
    - path: ^/profile
      roles: [ROLE_USER]
```

### Contrôleur d'authentification

```python
class SecurityController(AbstractController):
    @Route("/login", "security.login", methods=["GET", "POST"])
    async def login(self, request: Request):
        return self.render("security/login.html")
    
    @Route("/logout", "security.logout", methods=["GET"])
    async def logout(self):
        return self.redirect("security.login")
```

## Base de données et ORM

Définissez vos entités avec l'ORM intégré :

```python
from framefox.core.orm.entity import Entity
from framefox.core.orm.column import Column

class User(Entity):
    __tablename__ = "users"
    
    id = Column("id", primary_key=True, auto_increment=True)
    username = Column("username", type="VARCHAR(100)", nullable=False)
    email = Column("email", type="VARCHAR(255)", unique=True)
    created_at = Column("created_at", type="DATETIME", default="CURRENT_TIMESTAMP")
```

### Repository pattern

Créez des repositories pour l'accès aux données :

```python
from framefox.core.orm.repository import Repository

class UserRepository(Repository):
    def find_by_email(self, email: str):
        return self.find_one_by({"email": email})
    
    def find_active_users(self):
        return self.find_by({"active": True})
```

## Commandes du terminal

Framefox fournit un terminal interactif puissant :

```bash
# Générer un contrôleur
framefox create controller

# Générer une entité
framefox create entity

# Générer un CRUD complet
framefox create crud

# Lister toutes les routes
framefox debug router

# Vider le cache
framefox cache clear

# Lancer les tests
framefox test
```

## Middleware et événements

Framefox inclut plusieurs middlewares intégrés :

- **SessionMiddleware** : Gestion des sessions
- **CsrfMiddleware** : Protection CSRF
- **FirewallMiddleware** : Authentification et autorisation
- **RequestMiddleware** : Logs et profiling des requêtes

### Événements personnalisés

```python
from framefox.core.events.decorator.dispatch_event import DispatchEvent

class UserController(AbstractController):
    @Route("/users", "user.create", methods=["POST"])
    @DispatchEvent(event_before="user.before_create", event_after="user.after_create")
    async def create_user(self, request: Request):
        # Logique de création
        return {"success": True}
```

## Profiler de développement

En mode développement, accédez au profiler web sur `/_profiler` pour analyser :

- Informations sur les routes
- Performance des requêtes
- Logs et erreurs
- Utilisation mémoire
- Requêtes base de données

## Prochaines étapes

<CardGrid stagger>
	<Card title="Créer votre première app" icon="laptop">
		Suivez le tutoriel complet pour construire une application complète.
	</Card>
	<Card title="Explorer l'ORM" icon="database">
		Apprenez à modéliser vos données avec l'ORM intégré.
	</Card>
	<Card title="Sécuriser votre app" icon="shield">
		Configurez l'authentification et les autorisations.
	</Card>
	<Card title="Déployer en production" icon="rocket">
		Guide de déploiement et bonnes pratiques.
	</Card>
</CardGrid>

---

**Prêt à commencer ?** Framefox vous permet de développer rapidement des applications web Python modernes et maintenables. La philosophie du framework privilégie la productivité sans sacrifier la flexibilité !
